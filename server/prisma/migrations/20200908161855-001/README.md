# Migration `20200908161855-001`

This migration has been generated by IgweDaniel at 9/8/2020, 5:18:55 PM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "Doc" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "name" TEXT NOT NULL,
    "content" TEXT,
    "ownerId" INTEGER,

    FOREIGN KEY ("ownerId") REFERENCES "User"("id") ON DELETE SET NULL ON UPDATE CASCADE
)

CREATE TABLE "User" (
    "id" INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    "email" TEXT NOT NULL,
    "name" TEXT,
    "password" TEXT NOT NULL
)

CREATE TABLE "Collaboration" (
    "docId" INTEGER NOT NULL,
    "userId" INTEGER NOT NULL,
    "createdAt" DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,

    FOREIGN KEY ("docId") REFERENCES "Doc"("id") ON DELETE CASCADE ON UPDATE CASCADE,
    FOREIGN KEY ("userId") REFERENCES "User"("id") ON DELETE CASCADE ON UPDATE CASCADE,
PRIMARY KEY ("docId","userId")
)

CREATE UNIQUE INDEX "Doc.name_unique" ON "Doc"("name")

CREATE UNIQUE INDEX "User.email_unique" ON "User"("email")
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200908161855-001
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,38 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "sqlite"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model Doc {
+  id      Int             @id @default(autoincrement())
+  name    String          @unique
+  content String?
+  owner   User?           @relation(fields: [ownerId], references: [id])
+  ownerId Int?
+  collabs Collaboration[]
+}
+
+model User {
+  id         Int             @id @default(autoincrement())
+  email      String          @unique
+  name       String?
+  password   String
+  docs       Doc[]
+  sharedDocs Collaboration[]
+}
+
+model Collaboration {
+  doc       Doc      @relation(fields: [docId], references: [id])
+  docId     Int // relation scalar field (used in the `@relation` attribute above)
+  user      User     @relation(fields: [userId], references: [id])
+  userId    Int // relation scalar field (used in the `@relation` attribute above)
+  createdAt DateTime @default(now())
+  @@id([docId, userId])
+}
```


